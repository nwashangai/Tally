name: tally
description: "A small-business inventory tallying and stock-balancing application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State & Dependency Injection (Application/Presentation only)
  flutter_riverpod: ^2.6.1

  # Routing & Navigation
  go_router: ^14.8.1

  # Networking (Infrastructure only)
  dio: ^5.8.0+1

  # Network connectivity observation
  connectivity_plus: ^6.1.3

  # Secure token storage (Auth credentials/tokens)
  flutter_secure_storage: ^9.2.4

  # Local preferences (UI flags, selected store preference)
  shared_preferences: ^2.5.2

  # Local file paths (Documents directory for store storage files)
  path_provider: ^2.1.5

  # Deterministic Identifiers & Idempotency Keys
  uuid: ^4.5.1

  # Internationalization & formatting
  intl: ^0.20.2

  # Integrity & Checksum verification for snapshots
  crypto: ^3.0.6

  # Local Persistence (Drift + Encrypted SQLite via SQLCipher)
  drift: ^2.24.0
  sqlite3: ^2.4.6
  sqlcipher_flutter_libs: ^0.6.1
  sqlite3_flutter_libs: ^0.6.0+eol
  excel: ^4.0.6
  csv: ^8.0.0
  share_plus: ^12.0.2
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  drift_dev: ^2.24.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
